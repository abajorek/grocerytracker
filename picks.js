// Andy's Picks UI — paused. The script is still served by the backend
// and loads in the browser, but does nothing. Backend endpoints
// (/api/picks, /api/picks/feed, the per-pick filtering logic) remain
// available for diagnostics and any future re-enablement.
//
// To bring the chips back: revert this file to commit c6e57c9
// (or any earlier picks.js with `appendPickChips` / `selectPick`).

(function () { /* intentionally empty */ })();
